import plotMacros from 'graphics-component/utils/plot-macros';
import { module, test } from 'qunit';

module('Unit | Utility | plot macros');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = plotMacros();
  assert.ok(result);
});
