import mtcars from 'graphics-component/utils/mtcars';
import { module, test } from 'qunit';

module('Unit | Utility | mtcars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mtcars();
  assert.ok(result);
});
